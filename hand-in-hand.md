# 构建补丁手把手教程

这份说明只回答一个问题：`构建补丁` 这个按钮什么时候能点，以及点亮后该怎么操作。

## 什么时候能点

`构建补丁` 能点，需要同时满足下面两个条件：

1. 你已经在查看器里上传了至少一张有效的替换图。
2. 当前没有“已部署但未恢复”的补丁状态。

查看器内部的实际判断逻辑是：

```text
can_build = bool(plan) and not active_deployment
```

可以直接理解成：

- `plan` 不为空：系统已经根据你的替换图，成功定位到了至少一个要修改的 FHM2D 包。
- `active_deployment` 为空：当前游戏目录里没有一套仍处于部署状态的补丁。

## 最常见的几种状态

### 1. 按钮是灰的，因为还没有替换图

这时通常顶部状态会接近：

```text
0 张替换
0 个涉及包
```

只要你还没真正替换任何图，`构建补丁` 就不会亮。

### 2. 按钮会亮，因为已经有替换图

这时通常顶部状态会变成类似：

```text
1 张替换
1 个或以上涉及包
```

只要系统已经识别到替换图，并且能定位到对应 FHM2D 包，按钮就会亮。

### 3. 按钮重新变灰，因为你已经部署过补丁

如果你点过 `备份并部署`，但还没有点 `恢复备份`，那么查看器会把当前状态视为“游戏文件已经被补丁修改过”。  
这时 `构建补丁` 会再次变灰，防止连续叠加覆盖。

## 最短判断法

你可以只记这一句：

`我已经替换过至少一张图，而且当前没有未恢复的已部署补丁。`

满足这句，`构建补丁` 就应该能点。

## 手把手操作流程

## 一、先启动查看器

目录建议是：

```text
游戏目录/
  vsac29_Release.exe
  data/
  EXVS_Texture_Toolkit/
```

启动：

```text
启动立绘查看器.bat
```

如果已经做过一次完整建库，而且游戏资源与提取结果没有变化，后续通常不需要每次都重新跑一键全流程。

## 二、先看顶部状态区

打开查看器后，先看顶部和补丁区域的状态信息。

你重点看这几个量：

- `替换图数量`
- `涉及包数量`
- 当前是否处于 `已部署` / `可恢复` 状态

如果替换图数量还是 `0`，那现在不用看别的，`构建补丁` 一定是灰的。

## 三、选择一个角色并上传替换图

在查看器中：

1. 选中一个已经能正常预览的角色。
2. 找到要替换的图层，例如 `body`、`face`、`mouth`。
3. 上传你改好的 PNG 替换图。

这里有几个硬要求：

- 宽高必须和原图完全一致。
- 必须保留透明通道。
- 不要改画布大小。
- 建议继续使用 sRGB PNG。

上传成功后，查看器的组合预览应该立刻变化。  
只要预览已经变化，通常就说明 replacement 已经成功写入工作区。

## 四、确认按钮为什么没有亮

如果你已经上传图片，但 `构建补丁` 还是灰的，优先按下面顺序排查：

1. 你是否真的完成了替换上传，而不是只打开了图片。
2. 这张替换图是否因为尺寸或格式不对，被系统拒绝。
3. 当前资源是否缺少映射，导致系统找不到对应 FHM2D 包。
4. 你是否已经部署过补丁，但还没有恢复备份。
5. 后台是否还有正在运行的构建、部署或恢复任务。

## 五、按钮亮了以后怎么用

顺序固定就是三步。

### 第一步：构建补丁

点击 `构建补丁` 后，系统会：

1. 扫描当前全部替换图。
2. 自动定位这些替换图涉及的 FHM2D 包。
3. 在工作区生成修改后的补丁文件。

输出位置通常在：

```text
EXVS_Texture_Toolkit\workspace\patch-build\
```

## 六、构建完成后再部署

点击 `备份并部署` 之前，先确保游戏已经完全关闭。  
这里的“关闭”是指 `vsac*_Release.exe` 不能还在运行。

点击 `备份并部署` 后，系统会：

1. 先备份要替换的原始 FHM2D。
2. 校验源文件和构建结果的 SHA-256。
3. 将新补丁写入游戏目录。

如果游戏还开着，部署会被阻止。

## 七、需要回退时恢复备份

点击 `恢复备份` 后，系统会把最近一次仍处于部署状态的备份恢复回去。

这个按钮通常在下面情况会亮：

- 你已经成功部署过一次补丁。
- 这次部署对应的备份仍然存在。

## 一次最小可复现示例

如果你现在只是想把 `构建补丁` 点亮，可以按这个最短流程做：

1. 启动 `启动立绘查看器.bat`。
2. 选一组当前能正常预览的领航员。
3. 替换一张最简单的 `mouth` 或 `face` PNG。
4. 确认右侧或主预览里的叠图效果已经变化。
5. 看顶部状态是否出现：

```text
1 张替换
1 个或以上涉及包
```

6. 这时 `构建补丁` 就应该能点。

## 常见误区

### 误区 1：我已经有解包图片了，所以按钮应该能点

不对。  
只有“解包出来的原图”不算替换。必须实际上传过替换图，系统才会生成补丁计划。

### 误区 2：我部署完以后还能继续直接构建下一版

不建议，也被工具主动限制。  
已经部署但未恢复时，`构建补丁` 会被锁住。正确流程是先恢复备份，或者重新回到干净状态后再继续。

### 误区 3：只要上传了图，任何情况都会亮

不对。  
上传的图如果尺寸不匹配、映射缺失、或者当前有激活中的部署，按钮依然不会亮。

## 建议工作流

建议始终按这个顺序做：

1. 在查看器里上传替换图并先看预览。
2. 确认 `构建补丁` 已点亮。
3. 点 `构建补丁`。
4. 关闭游戏。
5. 点 `备份并部署`。
6. 进游戏测试。
7. 需要回退时点 `恢复备份`。

这样最稳定，也最容易排查问题。
